
export function nameValidator(username) {
  //const user = "admin"  
 

    if (!username) return "Name can't be empty."
    //if (name != user)return "Username Wrong. Please Try again"
    return ''
  }