export function emailValidator(email) {
    //const re = /\S+@\S+\.\S+/
    //const user = "admin"  

    if (!email) return "Email can't be empty."
    //if (!re.test(email)) return 'Ooops! We need a valid email address.'
    //if (email != user)return "Username Wrong. Please Try again"


    return ''
  }